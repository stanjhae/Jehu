/*
 * Delegates Messages
 *
 * This contains all the text for the Delegates component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Delegates';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Delegates component!',
  },
});
