/*
 * Sponsors Messages
 *
 * This contains all the text for the Sponsors component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Sponsors';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Sponsors component!',
  },
});
