/*
 * Transport Messages
 *
 * This contains all the text for the Transport component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Transport';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Transport component!',
  },
});
