/*
 * AboutDebmun Messages
 *
 * This contains all the text for the AboutDebmun component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AboutDebmun';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AboutDebmun component!',
  },
});
