/*
 * Process Messages
 *
 * This contains all the text for the Process component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Process';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Process component!',
  },
});
