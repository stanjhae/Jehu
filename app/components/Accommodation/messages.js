/*
 * Accommodation Messages
 *
 * This contains all the text for the Accommodation component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Accommodation';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Accommodation component!',
  },
});
